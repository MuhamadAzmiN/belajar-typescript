"use strict";
describe('tipe data', function () {
    it('hello', function () {
        const name = "Muhamad Azmi Naziyulloh";
        const umur = 20;
        const isVip = true;
        expect(name).toBe("Muhamad Azmi Naziyulloh");
        expect(umur).toBe(20);
        expect(isVip).toBe(true);
    });
});
